apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-vault-0
  labels:
    app: vault
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  volumeName: vault-pv  # 명시적으로 연결된 PV 지정
  storageClassName: standard  # 클러스터에서 사용하는 스토리지 클래스명
